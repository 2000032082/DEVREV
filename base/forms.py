# from django.forms import ModelForm
# from .models import Room

# class RoomForm(ModelForm):
#     class Meta:   #The class Meta: block is used to provide metadata for the RoomForm class. In this case, the metadata specifies that the form is associated with the Room model using the model = Room attribute. Any fields that should be included in the form are specified using the fields attribute.
#         model=Room
#         fields='__all__'  # includes all the fields